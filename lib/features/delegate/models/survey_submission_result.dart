class SurveySubmissionResult {
  const SurveySubmissionResult({
    required this.buildingId,
    required this.apartmentId,
    required this.familyId,
  });

  final int buildingId;
  final int apartmentId;
  final int familyId;
}
